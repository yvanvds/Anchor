<?xml version="1.0" encoding="utf-8"?>
<Package
  xmlns="http://schemas.microsoft.com/appx/manifest/foundation/windows10"
  xmlns:uap="http://schemas.microsoft.com/appx/manifest/uap/windows10"
  xmlns:uap5="http://schemas.microsoft.com/appx/manifest/uap/windows10/5"
  xmlns:rescap="http://schemas.microsoft.com/appx/manifest/foundation/windows10/restrictedcapabilities"
  IgnorableNamespaces="uap uap5 rescap">

  <Identity
    Name="net.arcadia.anchor.focusagent"
    Publisher="CN=Arcadia"
    Version="0.1.0.0" />

  <Properties>
    <DisplayName>Anchor Focus Agent</DisplayName>
    <PublisherDisplayName>Arcadia</PublisherDisplayName>
    <Logo>Assets\StoreLogo.png</Logo>
    <Description>Classroom focus agent. Keeps students on allowed apps during a focus session and reports activity to the teacher console.</Description>
  </Properties>

  <Dependencies>
    <TargetDeviceFamily Name="Windows.Desktop" MinVersion="10.0.17763.0" MaxVersionTested="10.0.26100.0" />
  </Dependencies>

  <Resources>
    <Resource Language="en-us" />
  </Resources>

  <Applications>
    <Application
      Id="FocusAgent"
      Executable="FocusAgent.App.exe"
      EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        DisplayName="Anchor Focus Agent"
        Description="Classroom focus agent"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png"
        BackgroundColor="transparent">
        <uap:DefaultTile
          Square310x310Logo="Assets\Square310x310Logo.png"
          Wide310x150Logo="Assets\Wide310x150Logo.png" />
        <uap:SplashScreen Image="Assets\SplashScreen.png" />
      </uap:VisualElements>
      <Extensions>
        <uap5:Extension
          Category="windows.startupTask"
          Executable="FocusAgent.App.exe"
          EntryPoint="Windows.FullTrustApplication">
          <uap5:StartupTask
            TaskId="AnchorFocusAgentStartup"
            Enabled="false"
            DisplayName="Anchor Focus Agent" />
        </uap5:Extension>
      </Extensions>
    </Application>
    <!-- #35: per-user watchdog. Same package identity as the App so it can
         read Package.Current to resolve the App's install path, and so MSIX
         uninstall removes both startup tasks together. AppListEntry="none"
         keeps it out of Start menu / Apps list — students should see one
         "Anchor Focus Agent" entry, not two. -->
    <Application
      Id="Watchdog"
      Executable="Watchdog\FocusAgent.Watchdog.exe"
      EntryPoint="Windows.FullTrustApplication">
      <uap:VisualElements
        AppListEntry="none"
        DisplayName="Anchor Focus Agent Watchdog"
        Description="Restarts Anchor Focus Agent if it stops unexpectedly"
        Square150x150Logo="Assets\Square150x150Logo.png"
        Square44x44Logo="Assets\Square44x44Logo.png"
        BackgroundColor="transparent" />
      <Extensions>
        <uap5:Extension
          Category="windows.startupTask"
          Executable="Watchdog\FocusAgent.Watchdog.exe"
          EntryPoint="Windows.FullTrustApplication">
          <uap5:StartupTask
            TaskId="AnchorFocusAgentWatchdogStartup"
            Enabled="false"
            DisplayName="Anchor Focus Agent Watchdog" />
        </uap5:Extension>
      </Extensions>
    </Application>
  </Applications>

  <Capabilities>
    <rescap:Capability Name="runFullTrust" />
    <Capability Name="internetClient" />
  </Capabilities>
</Package>
