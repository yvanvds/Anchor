# FocusAgent.Native

Isolated Win32 P/Invoke surface for the focus agent. Kept as its own assembly
so all native interop lives behind a single boundary (see
[focus-system-design.md §5.3](../../../focus-system-design.md)).

Empty placeholder for now — populated as foreground hook, focus pull-back, and
overlay management land in later issues.
